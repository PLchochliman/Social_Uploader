pip install requests Pillow




TO Use X, you need at firs generate account at: https://developer.twitter.com/en/apply-for-access
Then create a new app in your twitter account, after that you will get Consumer Key (API key), Consumer Secret (API secret) , Access token (
   access_token_key) and Access token_secret. After that, you can use this code.


To post on Facebook you need to create an developer account here: https://developers.facebook.com/ and register.
more about how to create token you cab find here https://aruljohn.com/blog/python-automate-facebook-posts/
REALLY IMPORTANT TO SIGN A USECASE TO POST, OTHERWISE IT WON'T WORK.
Remember to put how to remove data and handle data privacy. If you don't want to download at all, then provide facebook one.
Then after setting everything in .env file run FB_token_Extender. But at first you need a User Token. With this, using
FB_token_extender.py you can extend token at least 60 days. May be infinity, but it's not tested yet.











Then you need to install the following libraries:
    - pip install tweepy
    - pip install requests

